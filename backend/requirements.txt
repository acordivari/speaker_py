fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
pydantic==2.9.2
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
alembic==1.13.3
# Allows Pydantic v2 to evaluate `int | None` union syntax on Python 3.9.
# Render deploys on Python 3.11 (.python-version) where this is a no-op.
# Local dev runs on Python 3.9, which cannot evaluate X|Y unions at runtime.
eval_type_backport
